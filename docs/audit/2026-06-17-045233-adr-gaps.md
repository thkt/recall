# ADR Gaps Audit: 2026-06-17-045233

## Summary

| Metric                   | Value                                                        |
| ------------------------ | ------------------------------------------------------------ |
| Scope                    | recall / delta since 2026-06-14-154538 (6 changed src files) |
| Source files scanned     | 6                                                            |
| Documents scanned        | 0                                                            |
| Decision candidates      | 18                                                           |
| ADR-covered (excluded)   | 4                                                            |
| Net new candidates       | 14                                                           |
| ADR promotion candidates | 3                                                            |

This is a delta audit. The 2026-06-14-154538 run mined the stable core and its top three findings shipped as ADR-0001 (--json envelope freeze), ADR-0002 (persisted/emitted string tokens), ADR-0003 (DB 0o600). This run mines only the source files changed after that report's UTC stamp, pinned by commit (earliest post-stamp commit `4a96487`, 2026-06-14 16:33 UTC). The six changed non-test files are main.rs, search.rs, indexer.rs, db.rs, chunker.rs, ansi.rs; every other source file is carried unchanged from 2026-06-14 and not re-sectioned. No documents changed in the window, so Step 4 found 0 documents to scan. Each changed file was re-mined in full (not diff-only) so a decision's shifted significance in new surrounding code is caught. The four ADR-covered exclusions are findings now absorbed by ADR-0001/0002/0003 (envelope shape, string-token set membership, 0o600). Candidate E from the prior run (ansi.rs fast-path / char-loop agreement) is confirmed RESOLVED by #191 and excluded.

## Source File Decisions

### src/main.rs (3628 lines, doctor + status delta)

| #   | Line              | Decision                                                                                           | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ----------------- | -------------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 834-845, 944-948  | doctor/status agree on the health axis: not-installed model is FTS-only mode, never flips healthy  | Partial     | Yes                  | M      | medium        |
| 2   | 251-258, 239-243  | search marks degraded=true on no-model (coverage axis), intentionally diverging from doctor/status | Partial     | Yes                  | H      | medium        |
| 3   | 971-973, 985,1000 | doctor/status are read-only: never auto-create a DB; rests on `!path.exists()` ordering            | Partial     | Yes                  | H      | medium        |
| 4   | 862-868, 996-1015 | doctor "report on a broken index, never crash": check errors become a `fail` verdict, never abort  | Partial     | Yes                  | M      | medium        |
| 5   | 1319-1324, 1812   | KNOWN_SUBCOMMANDS must track the Command enum, else shorthand rewrites a subcommand to `search X`  | Partial     | Yes                  | H      | medium        |
| 6   | 871-875           | doctor uses quick_check not integrity_check (skips cross-index scan at 27k+ scale)                 | Yes         | No                   | M      | high          |
| 7   | 893-929           | orphan_embeddings/orphan_chunks integrity check (diagnostic; real invariant lives in db.rs)        | Yes         | No                   | M      | medium        |

### src/search.rs (745 lines)

| #   | Line             | Decision                                                                                            | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ---------------- | --------------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 374-385          | FTS hit resolves to its chunk via `m.rowid BETWEEN c.src_rowid_lo AND c.src_rowid_hi` + COUNT(\*)=1 | No          | Yes                  | H      | low           |
| 2   | 39-57, 676-683   | runtime vec_search failure swallowed to FTS-only, surfaced as envelope degraded=true (trigger rule) | Partial     | Yes                  | H      | medium        |
| 3   | 568-594, 622-633 | no-space CJK query split at script boundaries, adopted only if it yields a >=3-char anchor (#167)   | No          | Partial              | M      | high          |
| 4   | 651-660, 410-413 | all-short query yields fts_query=None but still runs vector search (#164)                           | No          | No                   | M      | high          |

### src/indexer.rs (787 lines)

| #   | Line             | Decision                                                                                             | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ---------------- | ---------------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 404-410, 524-536 | force/rebuild deletes only through source-keyed cleanup_orphans; blanket DELETE forbidden (#177)     | No          | Yes                  | H      | low           |
| 2   | 567-621          | any enumeration uncertainty (entry/read_dir failure, depth truncation) flips fully_read=false (#181) | No          | Yes                  | H      | low           |
| 3   | 478-482          | IncompleteEnumeration always emits even at preserved==0; MissingRoot suppresses at 0 (#185)          | No          | Yes                  | M      | medium        |
| 4   | 62-84            | per-cause reconcile/remediation note wording, framed as forward-reference (#183/#187)                | N/A (prose) | No                   | L      | high          |

### src/db.rs (238 lines)

| #   | Line    | Decision                                                                                         | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ------- | ------------------------------------------------------------------------------------------------ | ----------- | -------------------- | ------ | ------------- |
| 1   | 71-72   | src_rowid_lo/hi nullable INTEGER, NULL = legacy/instr-fallback tri-state, no CHECK constraint    | Partial     | Yes                  | H      | low           |
| 2   | 145-163 | migration ADD COLUMN keeps qa_chunks.id stable so vec_chunks embeddings survive without re-embed | Yes         | No                   | M      | low           |

### src/chunker.rs (144 lines)

| #   | Line    | Decision                                                                                        | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | ------- | ----------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 113-134 | split-siblings of one message share one rowid range (deliberate, rejected by COUNT(\*)=1 guard) | Yes         | No                   | H      | low           |
| 2   | 115-124 | `BETWEEN lo AND hi` correctness rests on messages.rowid being monotonic in conversation order   | Partial     | Yes                  | H      | low           |

### src/ansi.rs (63 lines)

| #   | Line      | Decision                                                                                        | Documented? | Incomplete-contract? | Impact | Reversibility |
| --- | --------- | ----------------------------------------------------------------------------------------------- | ----------- | -------------------- | ------ | ------------- |
| 1   | 26-34, 53 | fast-path keep predicate is identical text to the char loop, enforced by a property test (#191) | Yes         | No                   | H      | low           |

Prior-run candidate E (fast-path / char-loop agreement) was downgrade-to-comment in 2026-06-14. #191 delivered both the keep-in-sync comment (ansi.rs:26-33, :53-55) and the divergence property test `test_fast_path_and_char_loop_agree_on_control_chars` (ansi/tests.rs:26-45), and rewrote the fast-path to the same `chars().all(...)` predicate. The invariant is now tool-enforced. Gap RESOLVED, excluded from candidates.

## Prose Document Decisions

No documents changed in the audit window (post 2026-06-14-154538). README.md / README.ja.md / docs/PURPOSE.md were scanned in the prior run; not re-scanned. No decisions found in the delta.

## ADR Promotion Candidates (post-challenge)

| #   | Candidate                                                                                    | Initial | Challenge | Final          |
| --- | -------------------------------------------------------------------------------------------- | ------- | --------- | -------------- |
| D   | search.rs:39-57,676-683 - degraded envelope TRIGGER semantics (vec failure → degraded=true)  | promote | keep      | ADR            |
| E   | indexer.rs:404-410,567-621 - rebuild/force no-blanket-delete data-loss invariant (#177/#181) | promote | keep      | ADR            |
| G   | chunker/search/db/indexer - FTS-hit→chunk attribution via source-message rowid ranges (#192) | promote | keep      | ADR            |
| A   | main.rs:834-948,985,1000 - doctor/status no-model + read-only-no-auto-create agreement       | promote | downgrade | inline-comment |
| B   | main.rs:862-868,996-1015 - doctor "report, never crash" check error-routing                  | promote | downgrade | inline-comment |
| F   | indexer.rs:478-482 - skip-reason emit asymmetry (IncompleteEnumeration always surfaces)      | promote | downgrade | inline-comment |
| C   | main.rs:1319-1324 - KNOWN_SUBCOMMANDS ↔ Command enum sync                                    | promote | drop      | skip           |

Per-file summary: main.rs keep 0 / downgrade 2 (A, B) / drop 1 (C). search.rs keep 1 (D, shared with G). indexer.rs keep 1 (E) / downgrade 1 (F). chunker.rs + db.rs keep 1 (G, shared). ansi.rs: prior candidate E resolved (no new candidate).

## Hand-off

### ADR candidates (file via /adr)

| Candidate | One-line rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D         | ADR-0001 froze the `degraded` KEY, not its MEANING; the trigger spans load-time + runtime vec failure, and the no-model A/D axis asymmetry must be stated so a future contributor does not "reconcile" it into a bug. Best filed as a Consequences section extending ADR-0001, not standalone.                                                                                                                                                                                         |
| E         | Data-loss tier: a blanket `DELETE`-then-rescan rebuild (the obvious implementation #177 forbids) silently destroys the index on a transient root outage; reversal is unrecoverable. Binds future reset/export/reset-index delete paths no current test can reach. File as standalone ADR-0004 with cleanup_orphans source-keying + the fully_read conservatism (#181) in Consequences.                                                                                                 |
| G         | Four cross-module, schema-unenforceable invariants (range disjointness, lo/hi co-NULL tri-state, messages.rowid monotonic in conversation order, backfill id-order==insertion-order binding the chunker to a count-stable single pass) underwrite the `BETWEEN` JOIN; none is visible from a single file and no test reaches the chunker↔backfill coupling. File as one ADR; flag invariant (2) as a candidate for a schema CHECK `((src_rowid_lo IS NULL) = (src_rowid_hi IS NULL))`. |

### downgrade (comment-strengthening tasks, not ADRs)

| Candidate | Action                                                                                                                                                                                                                                                                                                                        |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A         | Cross-ref comment that doctor/status agree on the health axis (already comment+test+PURPOSE #5 covered). The candidate's three-way "degraded" agreement is FALSE — search marks no-model degraded by design — do not write it. Surface the missing status no-auto-create assertion as a test-gap follow-up below, not an ADR. |
| B         | Leave the existing module comment (main.rs:861-864, 996-999); error-routing is structurally enforced by `run_db_check` being the only DB-check entry and is test-covered. Impact is M (one subcommand), not H. Merge into A's comment if any diagnostic note is written at all.                                               |
| F         | One-line cross-ref in ADR-0002 noting the emit policy is type-enforced: the `SkippedReason` exhaustive match (indexer.rs:479-482) won't compile until a new variant's policy is chosen, and four tests pin it. No standalone ADR.                                                                                             |

### drop (recorded only) and bug-adjacent follow-ups

| Candidate | Disposition                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C         | DROP as ADR. The comment already existed and the bug (`recall doctor` → `search doctor`) shipped anyway — an ADR is a strictly weaker control than the comment that failed. Fix mechanically: a test iterating every `Command` variant asserting shorthand-expansion recognizes it, or derive KNOWN_SUBCOMMANDS from the enum to remove the second source of truth. Bug-fix/enforcement follow-up, not an ADR. |
| A (test)  | Test-gap follow-up: status has no equivalent of `doctor_reports_unhealthy_and_does_not_create_db_when_absent`; add `run_status_does_not_create_db_when_absent`. The read-only guarantee for status currently rests on an untested `!path.exists()` ordering.                                                                                                                                                   |

### Note on sequencing

This repo already has ADRs; the skill recommends running `/adrift` before `/census` to first scan existing ADRs for drift, then mine the gaps drift cannot see. This run went straight to mining. Consider `/adrift` against ADR-0001/0002/0003 as a separate pass.
