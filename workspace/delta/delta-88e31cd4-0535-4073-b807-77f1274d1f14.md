# Delta

SOW: none Generated: 2026-03-10T05:00:00Z

## Discoveries

- code-simplifier removes doc comments added for audit findings; tension resolved by accepting simplifier judgment for short self-documenting functions
- FTS5 `snippet()` cannot batch across session_ids — N+1 pattern is inherent to the API
- `SNIPPET_MAX_LEN` (chars) vs `EXCERPT_MAX_BYTES` (bytes) are different units, not duplicates
- `IndexOutcome::Skipped` was dead code — no consumer distinguished it from `Unchanged`
- FTS optimize at threshold=10 was O(total_index) on every incremental update

## Design Changes

| Original Plan                     | Actual                                    | Rationale                     |
| --------------------------------- | ----------------------------------------- | ----------------------------- |
| Separate `create_db_file` per cfg | Single function with inner `#[cfg(unix)]` | Eliminate copy-paste          |
| FTS optimize threshold=10         | threshold=500 or force                    | Avoid O(n) on small updates   |
| `IndexOutcome::Skipped` variant   | Merged into `Unchanged`                   | Dead variant                  |
| `SessionRow` intermediate struct  | Removed; inline tuple destructuring       | Redundant type                |
| 92 test functions                 | 65 via data-driven patterns               | Compact without coverage loss |
| `score_and_sort` unit tests       | Removed                                   | Covered by integration tests  |

## Decisions

- Param enum kept — `rusqlite::types::Value` lacks direct `ToSql` impl
- Exit code string matching kept — typed error enum is YAGNI for 2 cases
- `SessionData` public fields kept — private + getters is YAGNI for internal CLI
- `cleanup_orphans` early-exit: skip when indexed==0 && counts match
- `-` prefix added to sanitize_fts_query (SEC-001)
- BrokenPipe → exit(0) in print_result (SF-001)

## Pending

- [ ] Committed `5728467` on main; ready for push
- [ ] Audit interrupted — re-run `/audit` for final confirmation if desired
