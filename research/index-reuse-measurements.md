# 追記時の埋め込み再利用: 実測記録

2026-09-21。[Issue #325](https://github.com/thkt/recall/issues/325) のホスト実測。合意範囲・完了条件はIssueを正本とする。

## 対象と条件

- 変更前: main `e73b80499fdadbce524b20c6ffbf3ddd266dd472`（PR #334）。
- 測定した変更版: `92a8792ebe7ecf3def30b929e201154ef297b2e2`。引継ぎ候補 `47a818b7e82c1884d8d4a3f12994bd80095245da` は観測仕様の説明だけを追加し、Rust/Cargo入力39ファイルは測定版と完全一致する。
- Apple M3 / Mac15,3、RAM 24 GB、macOS 27.0、Rust 1.98.1。両版とも同じrelease条件。既存のホスト固有リンク問題に対して、両方に同じ `libclang_rt.osx.a` のlink引数を渡した。製品設定は変更していない。
- ruricoの固定モデル `cl-nagoya/ruri-v3-310m`、revision `18b60fb8c2b9df296fb4212bb7d23ef94e579cd3`。モデルファイルはcache済み、offline。token budget / forward pauseは既定値。
- 番号の異なる英語の合成Q&A 256往復、1ファイル155,684 bytes。1往復だけ追記して257チャンクにする。mtimeだけの更新では本文を変えない。私的ログ・本番DBは使用していない。
- 変更前→変更版を3回交互に測定。各回は新しいDBを作り、初回→無変更→mtimeのみ→追記の順で実行。各コマンドは別process、非TTY、stdout/stderrは分離。GPU・filesystem cache、電源状態、背景負荷は未制御。

## 結果

wallの中央値。括弧内は3回の最小–最大、単位は秒。推論入力数とバッチ回数は全3回で同じだった。

| ケース | 変更前 | 変更版 | 推論入力数 前→後 | バッチ回数 前→後 | 変更版の再利用数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 初回 | 12.536 (12.245–12.997) | 12.333 (12.328–12.734) | 256→256 | 2→2 | 0 |
| 無変更 | 0.685 (0.685–0.759) | 0.682 (0.679–0.801) | 0→0 | 0→0 | 0 |
| mtimeのみ | 12.282 (12.175–13.426) | 0.680 (0.677–0.812) | 256→0 | 2→0 | 256 |
| 1往復追記 | 12.345 (12.281–12.438) | 0.740 (0.717–0.750) | 257→1 | 3→1 | 256 |

バッチ回数はembedding APIの呼出し数で、別途行うモデルprobeと内部GPU forward数を含まない。変更前は正常終了・degradedなしを確認して保存件数とstderrの推論開始通知を読み、変更版では追加した観測値とも照合した。無変更ファイルは照合しないため、再利用カウンターも0となる。

追記の段階別中央値（秒）:

| 段階 | 変更前 | 変更版 |
| --- | ---: | ---: |
| モデルload/probe | 0.642242 | 0.639322 |
| 列挙 | 0.000084 | 0.000097 |
| 既存データ準備 | 0.000050 | 0.000052 |
| 本文解析 | 0.000446 | 0.000447 |
| FTS transaction | 0.015015 | 0.012024 |
| 内容照合 | 未実装 | 0.002146 |
| 未完了会話のチャンク化 | 0.002111 | 0.000008 |
| 未処理抽出 | 0.000087 | 0.000407 |
| 推論 | 11.617054 | 0.052376 |
| embedding保存 | 0.029775 | 0.000774 |

時間は重複するため合計しない。変更版のFTS transactionには列挙・準備・解析・照合も含む。変更前は列挙・準備が外側なので、FTSの数値を同一作業量として比較しない。transaction時間はロック待ちも含み、保持時間そのものではない。

## 保存内容と追加費用

- 全24実行が終了コード0、degradedなし。各ケースの本文・件数・検索用のsource rowid対応を照合した。最終状態は1会話、514メッセージ、257チャンク、257ベクトル。
- 変更版は既存256件のvector payloadをbyte単位で保持した。各反復の全ケースで、変更前と変更版のvector payloadのSHA-256も全件一致した。この合成入力での一致であり、一般的な検索品質評価ではない。
- SQLiteの割当/使用ページ容量は両版で同じ。初回4,194,304 bytes、mtime更新後5,042,176 bytes、追記後5,873,664 bytes。メッセージの置換費用は残る。最大RSSは両版とも約1.54–1.55 GBで、モデルmemoryの削減はしていない。
- 追記では内容照合約2.15 msに対し、推論は約11.62秒から約0.052秒になった。wall中央値は約94%減。今回の長い1会話への追記では追加費用に見合う。初回と無変更に大きな改善は見られず、モデルload/probeも残る。
- 照合は既存本文mapと埋め込み済みID集合を持ち、vec0を走査する。多数の変更会話・大きなDBや、同時writerの待ち時間はこの実測では評価していない。容量・全ワークロード・RSSの改善へ一般化しない。

## 測定対象の照合

変更版のbinary SHA-256: `826e6243cec4507bf35f68c95f74dadd4f2247fc769e76f21d9ddbc9e0f9214b`。変更前: `0cbc574413fbc07082bbfdf8e962e7af7c7e08f3724f82e1dba72c9f6b371fa0`。ホスト用測定scriptのSHA-256: `b4aade4e339e8b258563670a0947e281110c23472c4fce615cddadbe3def5a67`。raw JSON、stderr、各ケースのDB snapshot、39入力のhash、ビルド記録とscriptはホスト証拠に保持した。この表はホスト用scriptの測定であり、製品repoの計測scriptを実行した結果と混同しない。

変更版のRust/Cargo入力fingerprintは `81425c0b5527377a9864d3fd4362d60d0233e7b843a834f06a80d9260fc30aac`。作り方は `git ls-files '*.rs' Cargo.toml Cargo.lock` の39パスそれぞれのSHA-256を求め、パス順に `path + NUL + hash + newline` を連結したUTF-8 bytesをSHA-256にする。ビルド前後と引継ぎ候補で一致を確認した。提出版がこの入力と異なる場合、今回の時間をその版の実測とは扱わず、差分に応じて測定を更新する。

## 実装への引継ぎ

実装候補は `47a818b7e82c1884d8d4a3f12994bd80095245da` に保持している。main `e73b80499fdadbce524b20c6ffbf3ddd266dd472` から候補までの製品差分全体を取り込み、この報告だけを実装・レビュー対象にしない。候補は本文照合、同じwriter lock内での列挙と削除、規則版が不明な未完了会話の再解析を含む。単なるmtime skip、モデル不在時の保護、重複本文の部分成功、末尾変更、切詰め、世代照合の保証を維持する。

ホストでは関連62テストが成功した。規則版NULLの回帰シナリオは旧条件へ戻すと失敗し、修正後は成功した。通常の設定済みfull check、変更文書を含む独立評価、公開後の本文と同じheadのCI確認は、新しい検証対象で行う。過去runの成功・停止状態を引き継がず、Issueの完了条件や権限を変更しない。
